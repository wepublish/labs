-- History reconciliation placeholder. The actual SQL was applied to remote on
-- 2026-04-23 18:00:17 UTC. Body is byte-identical to
-- 20260423170000_hybrid_units_search.sql but was pushed under a rebased
-- timestamp via Supabase Studio — Supabase records both timestamps in
-- supabase_migrations.schema_migrations. This empty file lets `supabase db push`
-- agree that local + remote histories match. Re-running this migration is a
-- no-op because the schema changes are already live.
SELECT 1;
